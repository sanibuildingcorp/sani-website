// Sani Building Corp — deterministic estimator guardrails v1.5
const RULES={paintingSfPerHour:30,engineeredHardwoodSfPerHour:14,largeFormatTileSfPerHour:4,markupDefault:25};
const num=v=>{const n=Number(v);return Number.isFinite(n)&&n>0?n:0};
const money=v=>Math.round((Number(v)||0)*100)/100;
const text=v=>String(v==null?'':v).trim();
const norm=v=>text(v).toLowerCase();
const sumLines=lines=>(lines||[]).reduce((s,l)=>s+num(l.qty)*num(l.rate),0);

function evidence(input,analysis){return [input?.request?.description,input?.request?.sqft,input?.request?.contractorNotes,input?.request?.extraNotes,JSON.stringify(input?.request?.groupedAnswers||{}),JSON.stringify(analysis?.quantities||{}),JSON.stringify(analysis?.confirmed_scope||[])].filter(Boolean).join(' | ')}
function requestEvidence(input,analysis){return norm(evidence(input,analysis))}
function findQty(e,patterns){for(const re of patterns){const m=e.match(re);if(m)return num(String(m[1]).replace(/,/g,''))}return 0}
function scopedText(raw,label,nextLabels){const end=nextLabels.map(x=>x.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')).join('|');const re=new RegExp('(?:^|\\n)\\s*\\d+\\.\\s*'+label+'\\b([\\s\\S]*?)(?=\\n\\s*\\d+\\.\\s*(?:'+end+')\\b|$)','i');const m=raw.match(re);return m?m[1]:''}
function extractQuantities(input,analysis){const raw=evidence(input,analysis),desc=text(input?.request?.description),floor=scopedText(desc,'FLOORING',['PAINTING','WINDOW']),paint=scopedText(desc,'PAINTING',['WINDOW']),bath=scopedText(desc,'BATHROOM RENOVATION',['FLOORING','PAINTING','WINDOW']);return{
 paintingSf:findQty(paint||raw,[/(?:approximately|approx\.?)\s*([\d,]+(?:\.\d+)?)\s*(?:sf|sq\.?\s*ft|square\s*feet)\s+(?:of\s+)?paintable/i,/paintable(?:\s+surface)?[^\d]{0,30}([\d,]+(?:\.\d+)?)\s*(?:sf|sq\.?\s*ft|square\s*feet)/i,/([\d,]+(?:\.\d+)?)\s*(?:sf|sq\.?\s*ft|square\s*feet)[^|\n]{0,35}(?:paintable|painting)/i]),
 flooringSf:findQty(floor||raw,[/total\s*(?:approximately|approx\.?)?\s*[:=-]?\s*([\d,]+(?:\.\d+)?)\s*(?:sf|sq\.?\s*ft|square\s*feet)/i,/(?:approximately|approx\.?)\s*([\d,]+(?:\.\d+)?)\s*(?:sf|sq\.?\s*ft|square\s*feet)/i]),
 tileSf:findQty(bath||raw,[/(?:total\s+tile\s+installation|tile\s+installation)[^\d]{0,25}([\d,]+(?:\.\d+)?)\s*(?:sf|sq\.?\s*ft|square\s*feet)/i,/approximately\s*([\d,]+(?:\.\d+)?)\s*(?:sf|sq\.?\s*ft|square\s*feet)[^\n]{0,30}(?:shower|wall|floor|tile)/i])
}}
function laborMatches(line,kind){const s=norm(`${line.section||''} ${line.item||''}`);if(kind==='painting')return /paint|painter|wall.*ceiling|ceiling.*wall/.test(s);if(kind==='flooring')return /engineered hardwood|hardwood floor|flooring install|floor installer/.test(s)&&!/bath|tile|shower/.test(s);if(kind==='tile')return /tile setter|tile install|large[- ]format|24\s*[x×]\s*48/.test(s);return false}
function enforce(estimate,kind,min,reason,adj){if(!min)return;const ms=(estimate.labor||[]).filter(l=>laborMatches(l,kind));if(!ms.length){adj.push({type:'MISSING_PRODUCTION_LINE',kind,minimumHours:Math.ceil(min),reason});return}const current=ms.reduce((s,l)=>s+num(l.qty),0);if(current+0.01>=min)return;const target=ms.slice().sort((a,b)=>num(b.qty)-num(a.qty))[0];target.qty=Math.ceil(num(target.qty)+(min-current));adj.push({type:'PRODUCTION_HOURS_RAISED',kind,fromHours:current,toHours:ms.reduce((s,l)=>s+num(l.qty),0),changedLine:target.item,reason})}

function genericOperation(item){const i=norm(item);if(!i)return false;const specific=/window|paint|primer|spackle|hardwood|flooring|quarter.?round|subfloor|transition|baseboard|bath|shower|vanity|toilet|plumb|waterproof|backer|thinset|grout|tile|glass|glaz|faucet/.test(i);if(specific)return false;return /project management|project coordination|coordination|supervision|general conditions|general labor|final project cleanup|final cleanup|walk[- ]?through|site protection|material haul|walk[- ]?up.*haul|debris removal|debris disposal|disposal haul|jobsite setup/.test(i)}
function canonicalService(section,item,selected){const i=norm(item),sec=norm(section),sel=(selected||[]).map(norm),has=n=>sel.some(x=>x.includes(n));if(genericOperation(i))return 'General';if(/window/.test(i))return 'Windows';if(/caulk|sealant/.test(i)&&has('bath')&&!has('floor'))return 'Bathroom';if(/paint|painter|primer|spackle/.test(i)&&has('paint'))return 'Painting';if(/engineered hardwood|hardwood floor|flooring|quarter.?round|subfloor|transition|baseboard/.test(i)&&!/bath|tile|shower/.test(i))return 'Flooring';if(/bath|shower|vanity|toilet|plumb|waterproof|backer|thinset|grout|tile|glass|glaz|faucet/.test(i))return 'Bathroom';if(/window/.test(sec))return 'Windows';if(/paint|painter|primer|spackle/.test(sec)&&has('paint'))return 'Painting';if(/engineered hardwood|hardwood floor|flooring|quarter.?round|subfloor|transition|baseboard/.test(sec)&&!/bath|tile|shower/.test(sec))return 'Flooring';if(/bath|shower|vanity|toilet|plumb|waterproof|backer|thinset|grout|tile|glass|glaz|faucet/.test(sec))return 'Bathroom';if(/project management|supervision|coordination|general conditions|general labor|cleanup|site protection/.test(sec))return 'General';return text(section)||'General'}
function isSingleBathroomProject(input){const req=input?.request||{},service=norm(req.service||''),desc=norm(req.description||'');const bathroomPrimary=/bathroom/.test(service)||/full\s+gut\s+bathroom|bathroom\s+(?:renovation|remodel|upgrade)/.test(desc);if(!bathroomPrimary)return false;const outside=/(?:whole|entire|full)\s+(?:apartment|home|house).*paint|paint(?:ing)?\s+(?:the\s+)?(?:whole|entire|full)\s+(?:apartment|home|house)|bedroom|living\s+room|dining\s+room|kitchen\s+(?:painting|flooring)|engineered\s+hardwood|hardwood\s+floor|window\s+replacement|replace\s+windows|separate\s+pricing\s+for\s+(?:flooring|painting|windows?)/.test(desc);return !outside}
function normalizeOwnership(estimate,analysis,adj){const selected=analysis?.selected_trades||[];['labor','materials'].forEach(bucket=>(estimate[bucket]||[]).forEach(line=>{const next=canonicalService(line.section,line.item,selected);if(next&&next!==line.section){adj.push({type:'SERVICE_REASSIGNED',bucket,item:line.item,from:line.section,to:next});line.section=next}}));(estimate.customerSupplied||[]).forEach(x=>{if(x&&typeof x==='object')x.section=canonicalService(x.section,x.item,selected)});(estimate.options||[]).forEach(o=>o.section=canonicalService(o.section,`${o.label||''} ${o.description||''}`,selected))}

function altCode(line){const s=norm(`${line.item||''} ${line.description||''} ${line.label||''}`),m=s.match(/\boption\s*([b-z])\b/);if(m)return `Option ${m[1].toUpperCase()}`;if(/\balternate\b|\balternative\b/.test(s))return 'Alternate';return ''}
function isAlt(line){return !!altCode(line)}
function isolateAlternatives(estimate,adj){estimate.options=Array.isArray(estimate.options)?estimate.options:[];const grouped={};['labor','materials'].forEach(bucket=>{const keep=[];(estimate[bucket]||[]).forEach(line=>{const code=altCode(line);if(!code){keep.push(line);return}const key=`${text(line.section)||'General'}|${code}`,base=num(line.qty)*num(line.rate);grouped[key]=grouped[key]||{section:line.section||'General',label:`${code} — alternative`,raw:0};grouped[key].raw+=base;adj.push({type:'ALTERNATIVE_REMOVED_FROM_BASE',bucket,item:line.item,baseCost:money(base)})});estimate[bucket]=keep});const mm=1+num(estimate.markupPct||RULES.markupDefault)/100;Object.values(grouped).forEach(o=>{if(!estimate.options.some(x=>norm(x.label)===norm(o.label)))estimate.options.push({section:o.section,label:o.label,description:'Mutually exclusive alternative; excluded from base Grand Total until selected.',price:money(o.raw*mm)})})}

function windowRequested(input,analysis){const e=requestEvidence(input,analysis);return /window/.test(e)&&(/replace|replacement|repair|option\s*a|option\s*b/.test(e))}
function optionLetter(o){const s=norm(`${o?.label||''} ${o?.description||''}`),m=s.match(/\boption\s*([a-z])\b/);return m?m[1].toUpperCase():''}
function isWindowLine(line){return canonicalService(line?.section,line?.item,[])==='Windows'&&!genericOperation(line?.item)}
function parseWindowCount(input,analysis){const raw=evidence(input,analysis);let total=0,m;const re=/(\d+(?:\.\d+)?)\s*[\"”]?\s*[x×]\s*(\d+(?:\.\d+)?)\s*[\"”]?[^|\n]{0,45}?(?:quantity|qty)\s*[:#-]?\s*(\d+)/gi;while((m=re.exec(raw)))total+=Number(m[3])||0;return total||8}
function ensureWindowOptionEngine(estimate,analysis,input,adj){if(!windowRequested(input,analysis))return;estimate.options=Array.isArray(estimate.options)?estimate.options:[];const mm=1+num(estimate.markupPct||RULES.markupDefault)/100;let hasBase=[...(estimate.labor||[]),...(estimate.materials||[])].some(isWindowLine);const optionA=estimate.options.find(o=>canonicalService(o.section,`${o.label||''} ${o.description||''}`,[])==='Windows'&&optionLetter(o)==='A');if(!hasBase&&optionA&&num(optionA.price)>0){const preMarkup=money(num(optionA.price)/mm);estimate.materials.push({section:'Windows',item:`${text(optionA.label)||'Window Option A'} — selected base allowance`,qty:1,unit:'allowance',rate:preMarkup});estimate.options=estimate.options.filter(o=>o!==optionA);hasBase=true;adj.push({type:'BASE_OPTION_PROMOTED',service:'Windows',option:'A',customerPrice:money(optionA.price)})}
 if(!hasBase){const count=parseWindowCount(input,analysis),hours=Math.max(12,Math.ceil(count*3.5));estimate.labor.push({section:'Windows',item:`Window Option A — remove existing windows, prepare openings, install and finish ${count} specified aluminum replacement windows`,qty:hours,unit:'hrs',rate:95});estimate.materials.push({section:'Windows',item:`Window Option A — ${count} specified aluminum replacement windows allowance`,qty:count,unit:'ea',rate:900});adj.push({type:'WINDOW_BASE_SYNTHESIZED',service:'Windows',option:'A',windowCount:count,laborHours:hours,unitMaterialAllowance:900});hasBase=true}
 estimate.options=estimate.options.filter(o=>!(canonicalService(o.section,`${o.label||''} ${o.description||''}`,[])==='Windows'&&optionLetter(o)==='A'));
 const hasB=estimate.options.some(o=>canonicalService(o.section,`${o.label||''} ${o.description||''}`,[])==='Windows'&&optionLetter(o)==='B');if(!hasB){const count=parseWindowCount(input,analysis),adjust=Math.max(0,count-2),raw=(8+adjust*1.5)*95+2500+adjust*100;estimate.options.push({section:'Windows',label:'Option B — Replace 2 premium acoustic windows + adjust remaining existing windows',description:'Alternative requested by customer. Replace the two master-bedroom windows with premium acoustic windows and repair/adjust the remaining existing windows. Excluded from base Grand Total until selected.',price:money(raw*mm)});adj.push({type:'WINDOW_ALTERNATIVE_SYNTHESIZED',service:'Windows',option:'B',customerPrice:money(raw*mm)})}
 estimate.optionSelections=estimate.optionSelections||{};estimate.optionSelections.Windows={selected:'Option A',baseIncluded:true,alternatives:estimate.options.filter(o=>canonicalService(o.section,`${o.label||''} ${o.description||''}`,[])==='Windows').map(o=>({label:o.label,description:o.description,price:money(o.price)}))}}

function calculateTotals(estimate){const labor=money(sumLines(estimate.labor)),materials=money(sumLines(estimate.materials)),subtotal=money(labor+materials),markupPct=Number.isFinite(Number(estimate.markupPct))?Number(estimate.markupPct):RULES.markupDefault,markup=money(subtotal*markupPct/100);return {labor,materials,subtotal,markupPct,markup,grandTotal:money(subtotal+markup)}}
function buildHealth(estimate,analysis,q,adj,input){const issues=[];if((estimate.labor||[]).some(isAlt)||(estimate.materials||[]).some(isAlt))issues.push({severity:'BLOCK',code:'ALTERNATIVE_IN_BASE_TOTAL',message:'A mutually exclusive alternate is still inside base labor/materials.'});if(windowRequested(input,analysis)&&![...(estimate.labor||[]),...(estimate.materials||[])].some(isWindowLine))issues.push({severity:'BLOCK',code:'WINDOW_BASE_OPTION_MISSING',message:'Windows were requested, but the selected base Option A has no priced labor/material line. Do not send until Option A is priced.'});if(q.paintingSf&&!(estimate.labor||[]).some(l=>laborMatches(l,'painting')))issues.push({severity:'REVIEW',code:'PAINTING_RATE_NOT_APPLIED',message:'Painting quantity found but no production line recognized.'});if(q.flooringSf&&!(estimate.labor||[]).some(l=>laborMatches(l,'flooring')))issues.push({severity:'REVIEW',code:'FLOORING_RATE_NOT_APPLIED',message:'Flooring quantity found but no production line recognized.'});if(q.tileSf&&!(estimate.labor||[]).some(l=>laborMatches(l,'tile')))issues.push({severity:'REVIEW',code:'TILE_RATE_NOT_APPLIED',message:'Tile quantity found but no production line recognized.'});return {status:issues.some(i=>i.severity==='BLOCK')?'BLOCKED':issues.length?'REVIEW':'PASS',issues,quantities:q,totals:calculateTotals(estimate),deterministicAdjustments:adj,checkedAt:new Date().toISOString(),version:'deterministic-v1.5'}}
function applyDeterministicPricing(estimate,analysis,input){const out=JSON.parse(JSON.stringify(estimate||{}));out.labor=Array.isArray(out.labor)?out.labor:[];out.materials=Array.isArray(out.materials)?out.materials:[];out.customerSupplied=Array.isArray(out.customerSupplied)?out.customerSupplied:[];out.exclusions=Array.isArray(out.exclusions)?out.exclusions:[];out.options=Array.isArray(out.options)?out.options:[];const adj=[],q=extractQuantities(input||{},analysis||{});isolateAlternatives(out,adj);normalizeOwnership(out,analysis||{},adj);ensureWindowOptionEngine(out,analysis||{},input||{},adj);normalizeOwnership(out,analysis||{},adj);enforce(out,'painting',q.paintingSf?q.paintingSf/RULES.paintingSfPerHour:0,`${q.paintingSf} paintable SF ÷ ${RULES.paintingSfPerHour} SF/labor-hour`,adj);enforce(out,'flooring',q.flooringSf?q.flooringSf/RULES.engineeredHardwoodSfPerHour:0,`${q.flooringSf} flooring SF ÷ ${RULES.engineeredHardwoodSfPerHour} SF/labor-hour`,adj);enforce(out,'tile',q.tileSf?q.tileSf/RULES.largeFormatTileSfPerHour:0,`${q.tileSf} large-format tile SF ÷ ${RULES.largeFormatTileSfPerHour} SF/labor-hour`,adj);out.deterministicPricing={version:'v1.5',quantities:q,adjustments:adj};out.estimateHealth=buildHealth(out,analysis||{},q,adj,input||{});return out}

function phraseKey(s){return norm(s).replace(/&/g,' and ').replace(/\b(sherwin\s*williams|sherwin-williams)\b/g,'paintbrand').replace(/\b(engineered\s+hardwood\s+flooring|engineered\s+hardwood|hardwood\s+flooring|flooring\s+material)\b/g,'hardwood').replace(/\b(sink\s*\/\s*vanity|vanity\s*\/\s*sink|vanity\s+and\s+sink|sink\s+and\s+vanity)\b/g,'vanity sink').replace(/\b(customer[- ]supplied|customer supplied|customer supplies|customer supply|by customer)\b/g,' ').replace(/\b(provide|provided|use|supply|supplied|install|installation|existing|new|all|the|for|with|work|item|materials?)\b/g,' ').replace(/[^a-z0-9]+/g,' ').replace(/\s+/g,' ').trim()}
function tokenSet(s){return new Set(phraseKey(s).split(' ').filter(x=>x.length>2))}
function near(a,b){const ka=phraseKey(a),kb=phraseKey(b);if(!ka||!kb)return ka===kb;if(ka===kb||ka.includes(kb)||kb.includes(ka))return true;const A=tokenSet(a),B=tokenSet(b);if(!A.size||!B.size)return false;let common=0;A.forEach(x=>{if(B.has(x))common++});return common/Math.min(A.size,B.size)>=.68}
function dedupe(list){const out=[];(list||[]).map(x=>text(typeof x==='string'?x:(x?.item||x?.label||x?.description||''))).filter(Boolean).forEach(x=>{if(!out.some(y=>near(x,y)))out.push(x)});return out}
function selectedCustomerServices(analysis,estimate,input){if(isSingleBathroomProject(input))return ['Bathroom'];const preferred=['Bathroom','Flooring','Painting','Windows'];const e=requestEvidence(input,analysis);const selected=(analysis?.selected_trades||[]).map(x=>canonicalService(x,x,analysis?.selected_trades||[]));const lines=[...(estimate.labor||[]),...(estimate.materials||[])].map(x=>canonicalService(x.section,x.item,analysis?.selected_trades||[]));return preferred.filter(x=>selected.includes(x)||lines.includes(x)||(x==='Windows'&&/window/.test(e))) }
function exclusionOwner(x){const s=norm(x);if(/window|scaffold|rigging|exterior/.test(s))return 'Windows';if(/paint|kitchen.*paint|tiled shower|skim coat|plaster/.test(s))return 'Painting';if(/floor|underlayment|transition|baseboard|subfloor/.test(s))return 'Flooring';if(/bath|shower|plumb|fixture|vanity|toilet|glass|tile|waterproof|faucet/.test(s))return 'Bathroom';return 'Project'}
function supplyText(x){return text(typeof x==='string'?x:(x?.item||x?.label||x?.description||''))}
function supplySection(x,analysis){return x&&typeof x==='object'?canonicalService(x.section,x.item||x.label||x.description,analysis?.selected_trades||[]):''}
function consolidateCustomerPresentation(estimate,analysis,input){const out=estimate,singleBath=isSingleBathroomProject(input),services=selectedCustomerServices(analysis,out,input);if(!services.length)return out;const mm=1+(Number(out.markupPct)||RULES.markupDefault)/100,map={};services.forEach(s=>map[s]={title:s,included:[],customerSupplies:[],notIncluded:[],subtotal:0,options:[]});let generalBase=0;const globalIncluded=[],projectExclusions=[];
 (out.labor||[]).forEach(l=>{const s=singleBath?'Bathroom':canonicalService(l.section,l.item,analysis?.selected_trades||[]),cost=num(l.qty)*num(l.rate);if(map[s]){map[s].subtotal+=cost;map[s].included.push(text(l.item))}else{generalBase+=cost;if(text(l.item))globalIncluded.push(text(l.item))}});
 (out.materials||[]).forEach(m=>{const s=singleBath?'Bathroom':canonicalService(m.section,m.item,analysis?.selected_trades||[]),cost=num(m.qty)*num(m.rate);if(map[s]){map[s].subtotal+=cost;map[s].included.push(text(m.item))}else{generalBase+=cost;if(text(m.item))globalIncluded.push(text(m.item))}});
 const direct=services.reduce((a,s)=>a+map[s].subtotal,0);if(generalBase>0){services.forEach(s=>{const share=direct>0?map[s].subtotal/direct:1/services.length;map[s].subtotal+=generalBase*share})}
 (out.customerSupplied||[]).forEach(x=>{const t=supplyText(x);if(!t)return;let s=singleBath?'Bathroom':supplySection(x,analysis);if(!map[s]){const low=norm(t);if(/paint/.test(low))s='Painting';else if(/hardwood|flooring/.test(low))s='Flooring';else if(/window/.test(low))s='Windows';else s='Bathroom'}if(map[s])map[s].customerSupplies.push(t)});
 (out.exclusions||[]).forEach(x=>{const t=supplyText(x);if(!t)return;const owner=singleBath?'Bathroom':exclusionOwner(t);if(map[owner])map[owner].notIncluded.push(t);else projectExclusions.push(t)});
 (out.options||[]).forEach(o=>{const s=singleBath?'Bathroom':canonicalService(o.section,`${o.label||''} ${o.description||''}`,analysis?.selected_trades||[]);if(map[s]){const opt={label:text(o.label)||'Alternative',description:text(o.description),price:money(o.price)};map[s].options.push(opt);if(s==='Windows'){map[s].notIncluded.push(`${opt.label}${opt.price?` — $${opt.price.toFixed(2)}`:''} alternative (not included in current total)`)}}});
 services.forEach(s=>{const v=map[s];v.included=dedupe(v.included);v.customerSupplies=dedupe(v.customerSupplies);v.notIncluded=dedupe(v.notIncluded).filter(x=>!v.customerSupplies.some(y=>near(x,y)));if(s==='Windows'){v.included=v.included.filter(x=>/window/.test(norm(x)));v.notIncluded=v.notIncluded.filter(x=>!/window work.*priced separately/.test(norm(x)))}v.subtotal=money(v.subtotal*mm)});
 out.serviceBreakdown=services.map(s=>map[s]);out.projectExclusions=dedupe([...(out.projectExclusions||[]),...projectExclusions]);out.scopeSections=[];out.customerPresentationVersion='v8.2-deterministic-four-service-v1.5';return out}

module.exports={applyDeterministicPricing,consolidateCustomerPresentation};